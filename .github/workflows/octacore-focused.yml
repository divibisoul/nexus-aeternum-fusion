name: Octacore G3 Focused Gate

on:
  push:
    branches:
      - feat/octacore-g3-r1
  pull_request:
    branches:
      - main
    paths:
      - '**/*.ts'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'package-lock.json'
      - '.github/workflows/octacore-focused.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  focused:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: npm
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: TypeScript compile check
        run: npx tsc --noEmit
      - name: Octacore handler tests
        run: npx tsx api/octacore.test.ts
