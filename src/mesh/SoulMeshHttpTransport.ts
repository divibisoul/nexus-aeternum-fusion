import type { SoulMeshMessage } from './SoulMeshProtocol';
export type TransportState='closed'|'open'|'half-open';
export class SoulMeshHttpTransport { private failures=0; private state:TransportState='closed'; constructor(private readonly maxFailures=3,private readonly resetMs=10000){} async send(url:string,message:SoulMeshMessage,token?:string):Promise<Response>{ if(this.state==='open') throw new Error('MESH_CIRCUIT_OPEN'); const headers:Record<string,string>={'content-type':'application/json'}; if(token) headers.authorization=`Bearer ${token}`; let last:unknown; for(let attempt=0;attempt<3;attempt++){ try{const response=await fetch(url,{method:'POST',headers,body:JSON.stringify(message)}); if(response.ok){this.failures=0;this.state='closed';return response;} last=new Error(`MESH_HTTP_${response.status}`);}catch(error){last=error;} if(attempt<2) await new Promise(r=>setTimeout(r,250*(2**attempt))); } this.failures++; if(this.failures>=this.maxFailures){this.state='open';setTimeout(()=>{this.state='half-open';},this.resetMs);} throw last instanceof Error?last:new Error(String(last)); }}
